# CMake Modules

This directory contains custom CMake modules for the MarcSLM project.

## Purpose

Custom Find modules, utility functions, and build helpers that are not part of the standard CMake distribution.

Currently empty - will be populated as needed for:
- Custom dependency finding logic
- Platform-specific configuration
- Build utilities
